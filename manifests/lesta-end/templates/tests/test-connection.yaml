apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lesta-end.fullname" . }}-test-connection"
  labels:
    {{- include "lesta-end.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lesta-end.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
